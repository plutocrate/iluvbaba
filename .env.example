# This file is for reference only.
# Actual env config goes in server/.env (copy server/.env.example to server/.env)
#
# For Railway: set these as environment variables in your Railway service dashboard.
DATABASE_URL=postgres://user:password@host:5432/dbname
REDIS_URL=redis://localhost:6379
JWT_SECRET=change-me-to-something-random-and-long
PORT=3001
